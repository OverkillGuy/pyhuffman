"""An implementation of the LZW compression."""


def encode(msg: str) -> str:
    """Encode using LZW.

    Args:
        msg: The payload to encode.

    Returns:
        The encoded message.
    """
    _ = msg
    return ""


def decode(encoded: str) -> str:
    """Decode using LZW.

    Args:
        encoded: The payload to decode.

    Returns:
        The decoded message
    """
    return encoded
